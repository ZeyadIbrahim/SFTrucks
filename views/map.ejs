<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Map Page</title>
  </head>
  <body>
    <h1>map page</h1>

    <% for(i=0; i < apiData.data.length; i++) { %>
      <h3>ID: <%= apiData.data[i].id; %></h3>
    <% } %>

  </body>
</html>
